# npm dependencies — installed by `npm install`, regenerable from package-lock.json
node_modules/

# Vite build output (production bundle)
dist/

# Vite cache
.vite/

# Local env overrides
.env
.env.local
*.local

# Standalone HTML mockups / design archives — kept locally for reference
# but not part of the runtime app, so they stay out of the repo.
Skillswap.html
Skillswap.zip
